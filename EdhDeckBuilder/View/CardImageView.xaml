<UserControl x:Class="EdhDeckBuilder.CardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EdhDeckBuilder"
             xmlns:converters="clr-namespace:EdhDeckBuilder.View.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignData CardViewModel, IsDesignTimeCreatable=False}">
    <UserControl.Resources>
        <ResourceDictionary>
            <converters:ImageToBitmapSourceConverter x:Key="ImageConverter"/>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Image Source="{Binding CardImage, Converter={StaticResource ImageConverter}}" Width="265" Height="370"/>
    </Grid>
</UserControl>
