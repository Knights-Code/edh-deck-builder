<Window x:Class="EdhDeckBuilder.View.TagManagerView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EdhDeckBuilder.View"
        mc:Ignorable="d"
        WindowStartupLocation="Manual"
        WindowState="Maximized"
        Title="{Binding Title}" Height="700" Width="1000"
        d:DataContext="{d:DesignData TagManagerVm, IsDesignTimeCreatable=False}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Button Grid.Row="0" Command="{Binding RetrieveCommand}" IsEnabled="{Binding CanRetrieve}">
            Retrieve Scryfall tags and counts for deck
        </Button>
        <ScrollViewer Grid.Row="1">
            <TextBlock Grid.Row="1" Text="{Binding TagsSummary}" TextWrapping="Wrap" />
        </ScrollViewer>
        <Label Grid.Row="2" Content="{Binding Status}" />
    </Grid>
</Window>
